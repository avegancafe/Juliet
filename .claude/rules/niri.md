# Niri Configuration

## After Making Changes

**Always run `niri validate` after editing niri config** to check for errors like duplicate keybinds before reloading.

## Config Location

`symlinked/config/niri/config.kdl`
