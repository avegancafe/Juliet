# Included from ~/.ssh/config (juliet-bootstrap adds the Include line).
# Pin the dedicated auth key and offer only it, so strict servers don't
# reject the connection after the agent cycles through unrelated identities.
Host github.com gist.github.com
  User git
  IdentityFile ~/.ssh/github_auth
  IdentitiesOnly yes
